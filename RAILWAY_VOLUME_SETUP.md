# Railway Volume Setup Guide

## ปัญหาที่แก้ไข
ข้อมูลในระบบหายหลัง deploy เพราะ Railway ใช้ ephemeral filesystem ทำให้ไฟล์ `data.db` ถูกลบทุกครั้งที่อัปเดตโค้ด

## วิธีแก้ไข: ใช้ Railway Volume
Railway Volume จะเก็บข้อมูลแบบถาวร ไม่หายหลัง deploy

---

## 📚 หลักการทำงานของ Railway Volume

### เข้าใจปัญหา: Ephemeral Filesystem

Railway (และ cloud platform อื่นๆ) ใช้ **Container-based Deployment**:

```
┌─────────────────────────────────────────────┐
│  ทุกครั้งที่ Push Code ใหม่                 │
│                                             │
│  1. Railway สร้าง Container ใหม่           │
│  2. Copy โค้ดจาก Git เข้า Container         │
│  3. รัน app.py                              │
│  4. ลบ Container เก่าทิ้ง ❌                │
│                                             │
│  ผลลัพธ์: ไฟล์ data.db ที่อยู่ใน           │
│           Container เก่าหายไป!              │
└─────────────────────────────────────────────┘
```

**ปัญหา:**
- Container เป็น **Ephemeral** (ชั่วคราว) = ถูกลบทิ้งทุกครั้งที่ deploy
- ไฟล์ `data.db` ที่สร้างใน Container จะหายไปด้วย

---

### วิธีแก้ไข: Railway Volume (Persistent Storage)

Railway Volume คือ **พื้นที่เก็บข้อมูลแยกต่างหาก** ที่อยู่นอก Container:

```
┌──────────────────────────────────────────────────────────┐
│  Railway Infrastructure                                  │
│                                                           │
│  ┌─────────────────┐         ┌──────────────────────┐   │
│  │   Container     │         │  Railway Volume      │   │
│  │   (Ephemeral)   │         │  (Persistent)        │   │
│  │                 │         │                      │   │
│  │  /app/          │  Mount  │  /data/              │   │
│  │  ├── app.py     │ ◀────▶  │  └── data.db ✅      │   │
│  │  ├── models.py  │         │                      │   │
│  │  └── ...        │         │  ข้อมูลไม่หาย!      │   │
│  │                 │         │                      │   │
│  └─────────────────┘         └──────────────────────┘   │
│         ↓                              ↑                 │
│   Deploy ใหม่ = สร้างใหม่        Volume เดิม (ไม่ลบ)   │
└──────────────────────────────────────────────────────────┘
```

**หลักการ:**
1. **Volume** = พื้นที่เก็บข้อมูลถาวร (เหมือน External Hard Drive)
2. **Mount** = เชื่อมต่อ Volume เข้ากับ Container ที่ path ที่กำหนด (เช่น `/data`)
3. **Deploy ใหม่** → Container ใหม่ แต่ Volume เดิม → **ข้อมูลไม่หาย**

---

### ความสัมพันธ์ระหว่าง Mount Path, Environment Variable, และโค้ด

#### 1️⃣ **Mount Path** (ตั้งค่าใน Railway Volume)
```
Mount Path: /data
```
- Railway จะ **mount** Volume เข้ากับ Container ที่ path `/data`
- ทุกไฟล์ที่เขียนไปที่ `/data/*` จะถูกเก็บใน Volume (ไม่หาย)

#### 2️⃣ **Environment Variable** (ตั้งค่าใน Railway Variables)
```bash
RAILWAY_VOLUME_MOUNT_PATH=/data
```
- บอกโค้ดว่า "Volume ถูก mount ไว้ที่ไหน"
- ทำให้โค้ดรู้ว่าต้องเขียนไฟล์ไปที่ `/data`

#### 3️⃣ **โค้ดใน app.py** (อัตโนมัติ)
```python
# ตรวจสอบว่าอยู่ใน Railway และมี Volume หรือไม่
volume_path = os.environ.get("RAILWAY_VOLUME_MOUNT_PATH")
if volume_path:
    # Production (Railway with Volume)
    db_path = os.path.join(volume_path, "data.db")
    # ผลลัพธ์: /data/data.db ✅ (เก็บใน Volume)
else:
    # Local development
    db_path = os.path.join(os.path.dirname(__file__), "data.db")
    # ผลลัพธ์: ./data.db (เก็บในโฟลเดอร์โปรเจค)
```

---

### 🔄 Flow การทำงานทั้งหมด

#### **สถานการณ์ 1: ยังไม่ตั้งค่า Volume (ข้อมูลหาย)**
```
1. Deploy #1
   └─> สร้าง Container A
       └─> เขียน data.db ไว้ใน Container A (/app/data.db)
       └─> Import ข้อมูล 100 orders ✅

2. Push code ใหม่ (Deploy #2)
   └─> ลบ Container A ทิ้ง ❌
       └─> data.db หายไป (100 orders หายไป!)
   └─> สร้าง Container B ใหม่
       └─> data.db ว่างเปล่า (0 orders)
```

#### **สถานการณ์ 2: ตั้งค่า Volume แล้ว (ข้อมูลไม่หาย)**
```
1. สร้าง Volume
   └─> Railway สร้าง Volume (พื้นที่ถาวร)
   └─> Mount Volume ไว้ที่ /data

2. Set Environment Variable
   └─> RAILWAY_VOLUME_MOUNT_PATH=/data

3. Deploy #1
   └─> สร้าง Container A
   └─> อ่าน env var → รู้ว่า Volume อยู่ที่ /data
   └─> เขียน data.db ไปที่ /data/data.db (อยู่ใน Volume ✅)
   └─> Import ข้อมูล 100 orders ✅

4. Push code ใหม่ (Deploy #2)
   └─> ลบ Container A ทิ้ง
   └─> สร้าง Container B ใหม่
   └─> Mount Volume เดิม ไว้ที่ /data อีกครั้ง
   └─> อ่าน /data/data.db → พบข้อมูล 100 orders ✅✅✅
```

---

### ❓ คำถามที่พบบ่อย

**Q: Volume อยู่ใน Git หรือเปล่า?**
A: **ไม่!** Volume อยู่บน Railway Infrastructure ไม่เกี่ยวกับ Git
   - Git เก็บเฉพาะโค้ด (app.py, models.py, etc.)
   - Volume เก็บข้อมูล (data.db) บน Railway Cloud

**Q: ถ้าลบ Volume ข้อมูลจะเป็นอย่างไร?**
A: ข้อมูลจะหายทั้งหมด! Volume คือที่เก็บข้อมูลหลัก

**Q: ต้อง Backup Volume ไหม?**
A: **แนะนำให้ backup!** ดาวน์โหลด data.db มาเก็บไว้เป็นระยะ
   - ใช้ Railway CLI: `railway volume download`
   - หรือสร้างฟีเจอร์ Export Database ในเว็บ

**Q: ทำไมต้อง Set Environment Variable ด้วย?**
A: เพื่อให้โค้ดรู้ว่า Volume ถูก mount ไว้ที่ไหน
   - ถ้าไม่ set → โค้ดจะเขียนไปที่ `/app/data.db` (ใน Container = หาย)
   - ถ้า set → โค้ดจะเขียนไปที่ `/data/data.db` (ใน Volume = ไม่หาย)

**Q: Local Development ต้องตั้งค่าอะไรไหม?**
A: **ไม่ต้อง!** โค้ดจะตรวจสอบอัตโนมัติ:
   - ถ้าไม่มี `RAILWAY_VOLUME_MOUNT_PATH` → ใช้ path ปกติ (`./data.db`)
   - ถ้ามี → ใช้ Volume path (`/data/data.db`)

---

## ขั้นตอนการตั้งค่า Railway Volume

### 1. เข้าสู่ Railway Dashboard
- ไปที่ https://railway.app/
- เลือก Project ของคุณ

### 2. สร้าง Volume
1. คลิกที่ Service ของคุณ (ที่รัน app.py)
2. ไปที่แท็บ **"Variables"** หรือ **"Settings"**
3. เลื่อนลงหา **"Volumes"** section
4. คลิก **"+ New Volume"**

### 3. กำหนดค่า Volume
กรอกข้อมูลดังนี้:

- **Volume Name**: `data-volume` (ตั้งชื่ออะไรก็ได้)
- **Mount Path**: `/data`

> หมายเหตุ: ต้องใช้ `/data` ตามที่กำหนดไว้ในโค้ด

4. คลิก **"Add"** หรือ **"Create"**

### 4. ตั้งค่า Environment Variable
1. ไปที่แท็บ **"Variables"**
2. เพิ่ม Environment Variable ใหม่:
   ```
   RAILWAY_VOLUME_MOUNT_PATH = /data
   ```
3. คลิก **"Save"** หรือ **"Deploy"**

### 5. Deploy ใหม่
- Railway จะ redeploy อัตโนมัติ
- หรือกด **"Deploy"** ใหม่เอง

---

## วิธีตรวจสอบว่าใช้งานได้

### ตรวจสอบ Logs
1. ไปที่แท็บ **"Logs"** ใน Railway
2. หาข้อความที่แสดงว่า database path ถูกต้อง:
   ```
   Database path: /data/data.db
   ```

### ทดสอบการทำงาน
1. เข้าใช้งานระบบ
2. ตั้งค่า Google Sheet URL
3. Import ข้อมูล
4. Push code อัปเดตใหม่ (เช่นแก้ไข README)
5. รอ Railway deploy เสร็จ
6. เข้าระบบอีกครั้ง → ข้อมูลควรยังอยู่!

---

## การแก้ปัญหา (Troubleshooting)

### ถ้าข้อมูลยังหาย
1. ตรวจสอบว่า Environment Variable `RAILWAY_VOLUME_MOUNT_PATH` ตั้งค่าถูกต้อง
2. ตรวจสอบ Mount Path ใน Volume ต้องเป็น `/data`
3. ดู Logs ว่ามี error เกี่ยวกับ permission หรือไม่

### ถ้า Railway ไม่มี Volume option
- Railway Volume อาจไม่รองรับใน Free tier บางภูมิภาค
- พิจารณาเปลี่ยนไปใช้ Option 2 (PostgreSQL) แทน

---

## 🗑️ วิธีการล้าง Volume (Clear/Reset Volume)

### สถานการณ์: ต้องการเริ่มต้นใหม่ ลบข้อมูลทั้งหมด

มี **3 วิธี** ในการล้างข้อมูลใน Volume:

---

### **วิธีที่ 1: ใช้ฟีเจอร์ในแอพ (แนะนำ - ง่ายที่สุด)**

**ขั้นตอน:**
1. เข้าระบบที่ `https://your-app.railway.app`
2. Login เข้าสู่ระบบ
3. ไปที่หน้า **Import Orders**
4. เลื่อนลงล่างสุด หา **"ลบข้อมูลทั้งหมด"**
5. เลือก **"ล้างระบบใหม่หมด (All)"**
6. ยืนยันการลบ

**ผลลัพธ์:**
- ลบข้อมูล Orders, Products, Shops ทั้งหมด
- ล้างถังขยะ (Deleted Orders)
- ล้างประวัติการจ่ายงาน (Issued)
- ล้างประวัติการยกเลิก (Cancelled)
- **Volume ยังอยู่** (ไม่ต้องตั้งค่าใหม่)
- Database กลับมาเป็นว่างเปล่า

**ข้อดี:**
- ✅ ไม่ต้องลบ Volume
- ✅ ไม่ต้องตั้งค่าใหม่
- ✅ ทำได้ง่ายผ่านหน้าเว็บ
- ✅ ไม่เสียเวลา redeploy

**ข้อเสีย:**
- ⚠️ ต้อง login เข้าระบบ

---

### **วิธีที่ 2: ลบ Volume แล้วสร้างใหม่ (ตอบคำถามของคุณ)**

**ขั้นตอน:**

#### 1. ลบ Volume เก่า
1. ไปที่ Railway Dashboard → เลือก Service
2. ไปที่ **Settings** → **Volumes**
3. คลิก **Delete** ที่ Volume ที่ต้องการลบ
4. ยืนยันการลบ

**คำเตือน:** ข้อมูลทั้งหมดจะหายถาวร!

#### 2. สร้าง Volume ใหม่
1. ไปที่ **Settings** → **Volumes**
2. คลิก **+ New Volume**
3. กรอก:
   ```
   Volume Name: data-volume-new (หรือชื่ออะไรก็ได้)
   Mount Path: /data
   ```
4. คลิก **Create**

#### 3. ตั้งค่า Environment Variable (ถ้ายังไม่มี)
1. ไปที่ **Variables**
2. ตรวจสอบว่ามี `RAILWAY_VOLUME_MOUNT_PATH=/data` แล้วหรือไม่
3. ถ้ายังไม่มี → เพิ่มใหม่

#### 4. Deploy ใหม่
- Railway จะ redeploy อัตโนมัติ
- Volume ใหม่จะว่างเปล่า
- Database จะถูกสร้างใหม่

**ผลลัพธ์:**
- Volume ใหม่ ข้อมูลว่างเปล่า
- Database ถูกสร้างใหม่
- ต้อง Import ข้อมูลใหม่ทั้งหมด

**ข้อดี:**
- ✅ ลบข้อมูลสะอาดหมด
- ✅ Volume ใหม่เอี่ยม

**ข้อเสีย:**
- ⚠️ ต้องรอ redeploy (~2-3 นาที)
- ⚠️ ข้อมูลเก่าหายถาวร (ไม่สามารถกู้คืน)
- ⚠️ ต้องตั้งค่าใหม่ (ถ้าลืมเก็บ env var)

---

### **วิธีที่ 3: ใช้ Railway CLI (สำหรับ Advanced Users)**

**ขั้นตอน:**

1. ติดตั้ง Railway CLI:
   ```bash
   npm i -g @railway/cli
   ```

2. Login:
   ```bash
   railway login
   ```

3. Link โปรเจค:
   ```bash
   railway link
   ```

4. เข้าถึง Volume และลบไฟล์:
   ```bash
   railway run rm /data/data.db
   ```

5. Deploy ใหม่:
   ```bash
   railway up
   ```

**ผลลัพธ์:**
- ลบ `data.db` เท่านั้น
- Volume ยังอยู่
- Database จะถูกสร้างใหม่เมื่อ app เริ่มทำงาน

**ข้อดี:**
- ✅ ควบคุมได้แม่นยำ
- ✅ Volume ยังอยู่

**ข้อเสีย:**
- ⚠️ ต้องติดตั้ง CLI
- ⚠️ ซับซ้อนกว่า

---

### **เปรียบเทียบ 3 วิธี:**

| วิธี | ความยาก | Volume ยังอยู่? | ต้อง Redeploy? | แนะนำ |
|------|---------|----------------|----------------|-------|
| 1. ใช้ฟีเจอร์ในแอพ | ⭐ ง่าย | ✅ ใช่ | ❌ ไม่ต้อง | ✅ **แนะนำ** |
| 2. ลบ Volume แล้วสร้างใหม่ | ⭐⭐ ปานกลาง | ❌ ลบ | ✅ ต้อง | สำหรับเริ่มต้นใหม่หมด |
| 3. ใช้ Railway CLI | ⭐⭐⭐ ยาก | ✅ ใช่ | ✅ ต้อง | สำหรับ Advanced |

---

### **คำแนะนำ:**

**ถ้าต้องการล้างข้อมูล:**
→ **ใช้วิธีที่ 1** (ใช้ฟีเจอร์ในแอพ)

**ถ้าต้องการเริ่มต้นใหม่ทั้งหมด (รวมถึง Volume):**
→ **ใช้วิธีที่ 2** (ลบ Volume แล้วสร้างใหม่)

**ถ้าเป็น Developer ที่คุ้นเคย CLI:**
→ **ใช้วิธีที่ 3** (Railway CLI)

---

## 🔄 การ Sync Schema ใหม่จาก Local ไป Production

### สถานการณ์: เพิ่มตารางใหม่ใน Local → ต้องการ Sync ไป Railway

**ตัวอย่าง:**
- Local: เพิ่มตาราง `main` ใหม่ใน `models.py`
- Production (Railway): ยังไม่มีตาราง `main`
- **คำถาม:** จะทำอย่างไรให้ Production มีตาราง `main` เหมือน Local?

---

### **วิธีที่ 1: Auto-Migration (แนะนำ - ใช้ได้ถ้ามี db.create_all())**

โปรเจคนี้มี **Auto-Migration** อยู่แล้วใน `app.py`

**ขั้นตอน:**

1. **แก้ไข `models.py` ใน Local**
   ```python
   # เพิ่มตารางใหม่ใน models.py
   class Main(db.Model):
       __tablename__ = "main"
       id = db.Column(db.Integer, primary_key=True)
       name = db.Column(db.String(128))
       # ...
   ```

2. **ทดสอบใน Local**
   ```bash
   python app.py
   # ตรวจสอบว่าตาราง main ถูกสร้างแล้ว
   ```

3. **Commit และ Push ขึ้น Git**
   ```bash
   git add models.py
   git commit -m "Add new table: main"
   git push origin main
   ```

4. **Railway จะ Auto-Deploy**
   - Railway ดึง code ใหม่จาก Git
   - รัน `app.py`
   - `db.create_all()` จะสร้างตาราง `main` อัตโนมัติ
   - **ตารางใหม่ถูกเพิ่มเข้า Database ใน Volume**

5. **ตรวจสอบผล**
   - เข้า `/system-status` → ดูจำนวน records
   - หรือใช้ฟีเจอร์ที่เกี่ยวข้องกับตาราง `main`

**ผลลัพธ์:**
- ✅ ตาราง `main` ถูกสร้างใน Production
- ✅ ข้อมูลเก่ายังอยู่ครบ (ไม่หาย)
- ✅ ไม่ต้องทำอะไรเพิ่ม

**หมายเหตุ:**
- วิธีนี้ใช้ได้เฉพาะ **เพิ่มตารางใหม่** หรือ **เพิ่มคอลัมน์ใหม่**
- ถ้า **ลบตาราง** หรือ **แก้ไขโครงสร้าง** → ใช้วิธีที่ 2

---

### **วิธีที่ 2: Manual Migration ด้วย Railway CLI**

**ใช้เมื่อ:**
- Auto-migration ไม่ทำงาน
- ต้องการควบคุมการ migrate แบบละเอียด
- ต้องการรัน SQL commands โดยตรง

**ขั้นตอน:**

1. **ติดตั้ง Railway CLI**
   ```bash
   npm i -g @railway/cli
   ```

2. **Login และ Link Project**
   ```bash
   railway login
   railway link
   ```

3. **เข้าถึง Database ใน Volume**
   ```bash
   railway run sqlite3 /data/data.db
   ```

4. **เช็คว่ามีตารางอะไรบ้าง**
   ```sql
   .tables
   ```

5. **สร้างตารางใหม่ด้วย SQL**
   ```sql
   CREATE TABLE main (
       id INTEGER PRIMARY KEY AUTOINCREMENT,
       name VARCHAR(128)
   );
   ```

6. **ตรวจสอบ**
   ```sql
   .schema main
   .quit
   ```

7. **Deploy ใหม่ (ถ้าจำเป็น)**
   ```bash
   railway up
   ```

**ผลลัพธ์:**
- ✅ ตาราง `main` ถูกสร้างแล้ว
- ✅ ควบคุมได้แม่นยำ

**ข้อเสีย:**
- ⚠️ ซับซ้อน ต้องเขียน SQL เอง
- ⚠️ ต้องระวังเรื่อง data type ให้ตรงกับ SQLAlchemy

---

### **วิธีที่ 3: Reset Database (ถ้าข้อมูลเก่าไม่สำคัญ)**

**ใช้เมื่อ:**
- ข้อมูลเก่าไม่สำคัญ (เช่น development/staging)
- ต้องการเริ่มต้นใหม่หมด
- Schema เปลี่ยนแปลงมาก

**ขั้นตอน:**

1. **ลบ Database เก่า**
   ```bash
   railway run rm /data/data.db
   ```

2. **Push Code ใหม่**
   ```bash
   git push origin main
   ```

3. **Railway จะ Deploy**
   - Database ใหม่จะถูกสร้างพร้อมตารางทั้งหมด (รวม `main`)
   - ข้อมูลเป็น 0

4. **Import ข้อมูลใหม่**
   - เข้าหน้าเว็บ
   - Import Products, Orders, etc. ใหม่

**ผลลัพธ์:**
- ✅ Schema ใหม่ล่าสุด
- ⚠️ ข้อมูลเก่าหายหมด

---

### **เปรียบเทียบ 3 วิธี:**

| วิธี | ข้อมูลเก่า | ความยาก | Auto? | แนะนำ |
|------|-----------|---------|-------|-------|
| 1. Auto-Migration | ✅ ยังอยู่ | ⭐ ง่าย | ✅ ใช่ | ✅ **แนะนำ** |
| 2. Manual Migration | ✅ ยังอยู่ | ⭐⭐⭐ ยาก | ❌ ไม่ | สำหรับ Advanced |
| 3. Reset Database | ❌ หายหมด | ⭐⭐ ปานกลาง | ✅ ใช่ | Development only |

---

### **Flow ที่แนะนำ:**

```
1. แก้ไข models.py ใน Local
   └─> เพิ่มตาราง Main

2. ทดสอบใน Local
   └─> python app.py
   └─> ตรวจสอบว่าตารางถูกสร้าง

3. Commit และ Push
   └─> git add models.py
   └─> git commit -m "Add table: main"
   └─> git push origin main

4. Railway Auto-Deploy
   └─> ดึง code ใหม่
   └─> รัน app.py
   └─> db.create_all() สร้างตารางใหม่

5. ตรวจสอบใน Production
   └─> เข้า /system-status
   └─> ดูว่า Database ใช้งานได้ปกติ
```

---

### **ข้อควรระวัง:**

**การเปลี่ยนแปลง Schema ที่ปลอดภัย:**
- ✅ เพิ่มตารางใหม่
- ✅ เพิ่มคอลัมน์ใหม่ (ต้องมี default value หรือ nullable=True)

**การเปลี่ยนแปลง Schema ที่อันตราย:**
- ⚠️ ลบตาราง → ข้อมูลหาย
- ⚠️ ลบคอลัมน์ → ข้อมูลในคอลัมน์นั้นหาย
- ⚠️ เปลี่ยน data type → อาจเกิด error

**คำแนะนำ:**
- **Backup ก่อนเสมอ** ถ้าจะแก้ไข schema
- ใช้ **Migration Tools** เช่น Alembic (Flask-Migrate) สำหรับโปรเจคใหญ่
- ทดสอบใน **Local ก่อน** แล้วค่อย push

---

## ค่าใช้จ่าย
- Railway Volume: **$0.25/GB/month**
- ตัวอย่าง: database 10MB = ~$0.0025/month (ถูกมาก)

---

## สรุป
หลังจากตั้งค่าแล้ว:
- ข้อมูลจะไม่หายหลัง deploy
- ไม่ต้องตั้งค่า Google Sheet URL ใหม่
- ข้อมูล Import จะถูกเก็บไว้ถาวร

---

**สร้างโดย:** Claude Code
**วันที่:** 2025-12-13
